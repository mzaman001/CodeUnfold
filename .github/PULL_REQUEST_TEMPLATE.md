## What does this PR do?

<!-- One or two sentences. Link an issue if there is one. -->

## Checklist

- [ ] `python3 -m py_compile` passes on every file I touched
- [ ] `ruff check .` passes
- [ ] `pytest -v` passes
- [ ] I added/updated tests for the behavior I changed
- [ ] If I touched `main.py`'s widget layout or session-state handling, I did a live `streamlit run main.py` check, not just unit tests
- [ ] I updated relevant docs (`README.md`, `ARCHITECTURE.md`, `SECURITY.md`, or `AGENTS.md`) if this changes behavior they describe

## Anything reviewers should pay extra attention to?

<!-- Optional. E.g. "this touches the rate limiter, please double-check the peek-then-consume ordering still holds." -->
